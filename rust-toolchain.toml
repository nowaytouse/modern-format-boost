[toolchain]
# Pinned nightly for reproducible CI/local builds; bump intentionally (see docs/CHANGELOG.md).
channel = "nightly-2026-08-07"
components = ["rustfmt", "clippy", "llvm-tools"]
profile = "minimal"
