[build]
# Redirect Tauri/Cargo build output to the workspace root target directory
# so src-tauri does not maintain a separate 5 GB target tree.
# Path: crates/dev/src/vue/src-tauri/ → 5 levels up → workspace root
target-dir = "../../../../../target"
